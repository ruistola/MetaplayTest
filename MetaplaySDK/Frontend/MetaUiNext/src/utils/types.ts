/**
 * All the available color variants for UI components. Not all variants make sense for all component, so only a subset may be implemented.
 */
export type Variant = 'primary' | 'neutral' | 'success' | 'danger' | 'warning'
